<%# <!DOCTYPE html>
<%# <html>
  <%# <head> %>
    <%# <title>Rbnb</title> %>
    <%# <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"> %>
    <%#= csrf_meta_tags %>
    <%# <%= csp_meta_tag %>

    <%#= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%#= javascript_importmap_tags %>
  <%# </head>

  <%# <body> %>
    <%#= render "shared/navbar" %>

    <%# <div class="container"> %>
      <%#= yield %>
    <%# </div> %>
    <%#= render "shared/flashes" %>
  <%# </body>
</html> %>

<!DOCTYPE html>
<html>
  <head>
    <title>FlatBookingApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="<%= root_path %>">FlatBookingApp</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="<%= flats_path %>">Flats</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= new_flat_path %>">Add Flat</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container mt-4">
      <%= yield %>
    </div>
    <footer class="footer mt-auto py-3 bg-light">
      <div class="container">
        <span class="text-muted">© 2024 FlatBookingCompleteAwesomenessApp</span>
      </div>
    </footer>
  </body>
</html>
